{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:ui": "cd ~/Downloads/fullstackopen/part_2/notes && rm -fr dist && npm run build && cp -r dist ~/Downloads/render_test/notes",
    "deploy": "npm run build:ui && cd .. && git add notes && git commit -m 'deploying the notes app newest version' && git push",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Zacarias Casimiro",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.1"
  }
}
